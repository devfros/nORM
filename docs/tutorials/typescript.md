::: tip In progress
The TypeScript generator is under active development. Use [Getting started with Python](./python) for production-ready code generation today.
:::

When the TypeScript generator ships, its configuration will live under `gen.typescript` in `norm.yaml`. See [TypeScript configuration](../reference/configuration/typescript).
