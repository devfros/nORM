::: tip In progress
The Rust generator is under active development. Use [Getting started with Python](./python) for production-ready code generation today.
:::

When the Rust generator ships, its configuration will live under `gen.rust` in `norm.yaml`. See [Rust configuration](../reference/configuration/rust).
