::: tip In progress
The Go generator is under active development. Use [Getting started with Python](./python) for production-ready code generation today.
:::

When the Go generator ships, its configuration will live under `gen.go` in `norm.yaml`. See [Go configuration](../reference/configuration/go).
