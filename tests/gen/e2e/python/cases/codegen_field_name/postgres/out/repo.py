"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from psycopg import AsyncConnection as Connection
from .models import Bar


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def test(
        self,
    ) -> Bar | None:
        query = """
            SELECT
              bar.id AS id,
              bar."!!!nobody,_,-would-believe---this-...?!" AS "!!!nobody,_,-would-believe---this-...?!",
              bar."parent        id" AS "parent        id"
            FROM bar
            LIMIT 1
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = await cur.fetchone()
            if result is None:
                return None

            result = Bar(
                id=result[0],
                nobody_would_believe_this=result[1],
                parent_id=result[2],
            )

        return result
