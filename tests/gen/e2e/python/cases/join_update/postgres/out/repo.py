"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from psycopg import AsyncConnection as Connection


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def percentile(
        self,
    ) -> None:
        query = """
            UPDATE group_calc_totals AS gct SET npn = nem.npn
            FROM producer_group_attribute AS ga
              JOIN npn_external_map AS nem
                ON ga.npn_external_map_id = nem.id
            WHERE
              gct.group_id = ga.group_id
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)

            result = None

        return result
