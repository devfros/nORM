"""
Generated by nORM. READ ONLY.
https://github.com/devfros/norm
"""

from asyncmy import Connection
from ._utils.sentinel import UNSET


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def non_dynamic(
        self,
        name: str,
        rating: int | None,
    ) -> int:
        query = """
            UPDATE authors SET name = %s, rating = %s
        """

        params = [
            name,
            rating,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = cur.lastrowid

        return result

    async def optional_name(
        self,
        rating: int | None,
        name: str = UNSET,
    ) -> int:
        query = """
            UPDATE authors SET name = CASE
              WHEN %s AND NOT CAST(%s AS CHAR) IS NULL
              THEN %s
              ELSE name
            END, rating = %s
        """

        params = [
            UNSET.to_bool(name),
            UNSET.to_none(name),
            UNSET.to_none(name),
            rating,
        ]

        # Clear sentinel values
        params = [
            p for p in params if p is not UNSET
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = cur.rowcount

        return result

    async def optional_rating(
        self,
        name: str,
        rating: int | None = UNSET,
    ) -> int:
        query = """
            UPDATE authors SET name = %s, rating = CASE WHEN %s THEN %s ELSE rating END
        """

        params = [
            name,
            UNSET.to_bool(rating),
            UNSET.to_none(rating),
        ]

        # Clear sentinel values
        params = [
            p for p in params if p is not UNSET
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = cur.rowcount

        return result

    async def full_optional(
        self,
        name: str = UNSET,
        rating: int | None = UNSET,
    ) -> None:
        query = """
            UPDATE authors SET name = CASE
              WHEN %s AND NOT CAST(%s AS CHAR) IS NULL
              THEN %s
              ELSE name
            END, rating = CASE WHEN %s THEN %s ELSE rating END
        """

        params = [
            UNSET.to_bool(name),
            UNSET.to_none(name),
            UNSET.to_none(name),
            UNSET.to_bool(rating),
            UNSET.to_none(rating),
        ]

        # Clear sentinel values
        params = [
            p for p in params if p is not UNSET
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result
