"""
Generated by nORM. READ ONLY.
https://github.com/devfros/norm
"""

from aioduck import AsyncConnection as Connection
from ._utils.sentinel import UNSET
from .models import Author


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def non_dynamic(
        self,
        name: str,
        rating: int | None,
    ) -> Author | None:
        query = """
            UPDATE authors SET name = ?, rating = ?
            RETURNING authors.id AS id, authors.name AS name, authors.rating AS rating
        """

        params = [
            name,
            rating,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = Author(
                id=result[0],
                name=result[1],
                rating=result[2],
            )

        return result

    async def optional_name(
        self,
        rating: int | None,
        name: str = UNSET,
    ) -> None:
        query = """
            UPDATE authors SET name = CASE
              WHEN ? AND NOT CAST(? AS TEXT) IS NULL
              THEN ?
              ELSE name
            END, rating = ?
        """

        params = [
            UNSET.to_bool(name),
            UNSET.to_none(name),
            UNSET.to_none(name),
            rating,
        ]

        # Clear sentinel values
        params = [
            p for p in params if p is not UNSET
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def optional_rating(
        self,
        name: str,
        rating: int | None = UNSET,
    ) -> None:
        query = """
            UPDATE authors SET name = ?, rating = CASE WHEN ? THEN ? ELSE rating END
        """

        params = [
            name,
            UNSET.to_bool(rating),
            UNSET.to_none(rating),
        ]

        # Clear sentinel values
        params = [
            p for p in params if p is not UNSET
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def full_optional(
        self,
        name: str = UNSET,
        rating: int | None = UNSET,
    ) -> None:
        query = """
            UPDATE authors SET name = CASE
              WHEN ? AND NOT CAST(? AS TEXT) IS NULL
              THEN ?
              ELSE name
            END, rating = CASE WHEN ? THEN ? ELSE rating END
        """

        params = [
            UNSET.to_bool(name),
            UNSET.to_none(name),
            UNSET.to_none(name),
            UNSET.to_bool(rating),
            UNSET.to_none(rating),
        ]

        # Clear sentinel values
        params = [
            p for p in params if p is not UNSET
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result
