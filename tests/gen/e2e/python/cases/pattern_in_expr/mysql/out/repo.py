"""
Generated by nORM. READ ONLY.
https://github.com/devfros/norm
"""

from asyncmy import Connection
from .models import Foo


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def foo_by_bar_b(
        self,
        b: str,
    ) -> list[Foo]:
        query = """
            SELECT
              foo.a AS a,
              foo.b AS b
            FROM foo
            WHERE
              foo.a IN (
                SELECT
                  bar.a AS a
                FROM bar
                WHERE
                  bar.b = %s
              )
        """

        params = [
            b,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                Foo(
                    a=item[0],
                    b=item[1],
                )
                async for item in cur
            ]

        return result

    async def foo_by_list(
        self,
        a1: str,
        a2: str,
    ) -> list[Foo]:
        query = """
            SELECT
              foo.a AS a,
              foo.b AS b
            FROM foo
            WHERE
              foo.a IN (%s, %s)
        """

        params = [
            a1,
            a2,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                Foo(
                    a=item[0],
                    b=item[1],
                )
                async for item in cur
            ]

        return result

    async def foo_by_not_list(
        self,
        a1: str,
        a2: str,
    ) -> list[Foo]:
        query = """
            SELECT
              foo.a AS a,
              foo.b AS b
            FROM foo
            WHERE
              NOT foo.a IN (%s, %s)
        """

        params = [
            a1,
            a2,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                Foo(
                    a=item[0],
                    b=item[1],
                )
                async for item in cur
            ]

        return result

    async def foo_by_param_list(
        self,
        a: str,
    ) -> list[Foo]:
        query = """
            SELECT
              foo.a AS a,
              foo.b AS b
            FROM foo
            WHERE
              %s IN (foo.a, foo.b)
        """

        params = [
            a,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                Foo(
                    a=item[0],
                    b=item[1],
                )
                async for item in cur
            ]

        return result
