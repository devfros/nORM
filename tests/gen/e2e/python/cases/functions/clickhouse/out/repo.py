"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from clickhouse_driver.dbapi.connection import Connection
from contextlib import closing
from typing import Any


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    def call_add(
        self,
        a: int,
        b: int,
    ) -> Any | None:
        query = """
            SELECT
              add(
                CAST(%(a)s AS Nullable(Int32)),
                CAST(%(b)s AS Nullable(Int32))
              ) AS _col_0
        """

        params = {
            "a": a,
            "b": b,
        }

        with closing(self.db.cursor()) as cur:
            cur.execute(query, params)
            result = cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result

    def call_mul(
        self,
        a: int,
    ) -> int | None:
        query = """
            SELECT
              mul(CAST(%(a)s AS Nullable(Int32))) AS _col_0
        """

        params = {
            "a": a,
        }

        with closing(self.db.cursor()) as cur:
            cur.execute(query, params)
            result = cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result
