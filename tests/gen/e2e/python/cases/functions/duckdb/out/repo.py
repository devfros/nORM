"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from contextlib import closing
from duckdb import DuckDBPyConnection as Connection
from typing import Any


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    def call_add(
        self,
        a: int,
        b: int,
    ) -> Any | None:
        query = """
            SELECT
              ADD(CAST(? AS INT), CAST(? AS INT)) AS _col_0
        """

        params = [
            a,
            b,
        ]

        with closing(self.db.cursor()) as cur:
            cur.execute(query, params)
            result = cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result

    def call_typed(
        self,
        x: int,
    ) -> Any | None:
        query = """
            SELECT
              main.typed(CAST(? AS INT)) AS typed
        """

        params = [
            x,
        ]

        with closing(self.db.cursor()) as cur:
            cur.execute(query, params)
            result = cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result
