"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from psycopg import AsyncConnection as Connection
from pydantic import BaseModel


# Query row schemas
class ListReturnsTableRow(BaseModel):
    id: int | None
    name: str | None


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def call_foo_text(
        self,
        bar: str,
    ) -> bool | None:
        query = """
            SELECT
              app.foo(CAST(%(bar)s AS TEXT)) AS foo
        """

        params = {
            "bar": bar,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result

    async def call_foo_int(
        self,
        bar: int,
    ) -> str | None:
        query = """
            SELECT
              app.foo(CAST(%(bar)s AS INT)) AS foo
        """

        params = {
            "bar": bar,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result

    async def call_add(
        self,
        a: int,
        b: int,
    ) -> int | None:
        query = """
            SELECT
              app.add(%(a)s, %(b)s) AS add
        """

        params = {
            "a": a,
            "b": b,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result

    async def list_returns_table(
        self,
        x: int,
    ) -> list[ListReturnsTableRow]:
        query = """
            SELECT
              id AS id,
              name AS name
            FROM app.RETURNS_TABLE(%(x)s)
        """

        params = {
            "x": x,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                ListReturnsTableRow(
                    id=item[0],
                    name=item[1],
                )
                async for item in cur
            ]

        return result
