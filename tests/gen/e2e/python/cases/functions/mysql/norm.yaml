version: "1"

targets:
  - name: default

    sql:
      engine: mysql
      db_schema: ./in/schema.sql
      repositories: ./in/repos

    gen:
      out: ./out
      python:
        asynchronous: false
        models: pydantic
        max_params: 10
