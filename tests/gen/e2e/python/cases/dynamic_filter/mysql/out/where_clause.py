"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from asyncmy import Connection
from ._utils.dynamic_filter import build_filter_clauses
from ._utils.sentinel import UNSET
from .models import Author


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def non_dynamic(
        self,
        id: int,
        rating: int,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors
            WHERE
              authors.id = %s OR authors.rating > %s
        """

        params = [
            id,
            rating,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                async for item in cur
            ]

        return result

    async def optional_id(
        self,
        rating: int,
        id: int = UNSET,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors /*FILTER1*/
        """

        params = [
            id,
            rating,
        ]

        # DYNAMIC FILTER
        query = build_filter_clauses(
            query,
            {
              "id": id,
            },
            {
              "/*FILTER1*/": {'clause': 'WHERE', 'tree': {'t': 'OR', 'l': {'p': ['id'], 'v': 'authors.id = %s'}, 'r': {'v': 'authors.rating > %s'}}},
            }
        )

        # Clear sentinel values
        params = [
            p for p in params if p is not UNSET
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                async for item in cur
            ]

        return result

    async def optional_rating(
        self,
        id: int,
        rating: int = UNSET,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors /*FILTER1*/
        """

        params = [
            id,
            rating,
        ]

        # DYNAMIC FILTER
        query = build_filter_clauses(
            query,
            {
              "rating": rating,
            },
            {
              "/*FILTER1*/": {'clause': 'WHERE', 'tree': {'t': 'OR', 'l': {'v': 'authors.id = %s'}, 'r': {'p': ['rating'], 'v': 'authors.rating > %s'}}},
            }
        )

        # Clear sentinel values
        params = [
            p for p in params if p is not UNSET
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                async for item in cur
            ]

        return result

    async def full_optional(
        self,
        id: int = UNSET,
        rating: int = UNSET,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors /*FILTER1*/
        """

        params = [
            id,
            rating,
        ]

        # DYNAMIC FILTER
        query = build_filter_clauses(
            query,
            {
              "id": id,
              "rating": rating,
            },
            {
              "/*FILTER1*/": {'clause': 'WHERE', 'tree': {'t': 'OR', 'l': {'p': ['id'], 'v': 'authors.id = %s'}, 'r': {'p': ['rating'], 'v': 'authors.rating > %s'}}},
            }
        )

        # Clear sentinel values
        params = [
            p for p in params if p is not UNSET
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                async for item in cur
            ]

        return result

    async def with_list_macro(
        self,
        ratings: list[int],
        id: int = UNSET,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors /*FILTER1*/
        """

        params = [
            id,
            *ratings,
        ]

        # DYNAMIC FILTER
        query = build_filter_clauses(
            query,
            {
              "id": id,
            },
            {
              "/*FILTER1*/": {'clause': 'WHERE', 'tree': {'t': 'OR', 'l': {'p': ['id'], 'v': 'authors.id = %s'}, 'r': {'v': 'authors.rating IN (/*LIST1*/)'}}},
            }
        )

        # LISTS
        query = query.replace(
            "/*LIST1*/", ", ".join("%s" for _ in ratings)
        )

        # Clear sentinel values
        params = [
            p for p in params if p is not UNSET
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                async for item in cur
            ]

        return result

    async def with_narg_macro(
        self,
        rating: int = UNSET,
        id: int | None = UNSET,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors /*FILTER1*/
        """

        params = [
            id,
            rating,
        ]

        # DYNAMIC FILTER
        query = build_filter_clauses(
            query,
            {
              "id": id,
              "rating": rating,
            },
            {
              "/*FILTER1*/": {'clause': 'WHERE', 'tree': {'t': 'OR', 'l': {'p': ['id'], 'v': 'authors.id = %s'}, 'r': {'p': ['rating'], 'v': 'authors.rating = %s'}}},
            }
        )

        # Clear sentinel values
        params = [
            p for p in params if p is not UNSET
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                async for item in cur
            ]

        return result
