"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from dataclasses import dataclass
from datetime import datetime
from psycopg import AsyncConnection as Connection
from .models import Author


# Query param schemas
@dataclass
class CreateParams:
    name: str
    rating: int | None


@dataclass
class UpdateOneParams:
    name: str
    rating: int | None
    id: int


@dataclass
class UpdateManyParams:
    name: str
    rating: int | None


# Query row schemas
@dataclass
class GetWithBooksRow:
    id: int
    name: str
    rating: int | None
    id_2: int
    author_id: int
    isbn: str
    book_type: str
    title: str
    year: int
    available: datetime
    tags: list[str]


@dataclass
class PartialRow:
    name: str
    rating: int | None


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def get_one(
        self,
        id: int,
    ) -> Author | None:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors
            WHERE
              authors.id = %(id)s
        """

        params = {
            "id": id,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = Author(
                id=result[0],
                name=result[1],
                rating=result[2],
            )

        return result

    async def get_many(
        self,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                async for item in cur
            ]

        return result

    async def get_with_books(
        self,
    ) -> list[GetWithBooksRow]:
        query = """
            SELECT
              a.id AS id,
              a.name AS name,
              a.rating AS rating,
              b.id AS id,
              b.author_id AS author_id,
              b.isbn AS isbn,
              b.book_type AS book_type,
              b.title AS title,
              b.year AS year,
              b.available AS available,
              b.tags AS tags
            FROM authors AS a
            JOIN books AS b
              ON b.author_id = a.id
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                GetWithBooksRow(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                    id_2=item[3],
                    author_id=item[4],
                    isbn=item[5],
                    book_type=item[6],
                    title=item[7],
                    year=item[8],
                    available=item[9],
                    tags=item[10],
                )
                async for item in cur
            ]

        return result

    async def create(
        self,
        arg: CreateParams,
    ) -> Author | None:
        query = """
            INSERT INTO authors (
              name,
              rating
            )
            VALUES
              (%(name)s, %(rating)s)
            RETURNING authors.id AS id, authors.name AS name, authors.rating AS rating
        """

        params = {
            "name": arg.name,
            "rating": arg.rating,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = Author(
                id=result[0],
                name=result[1],
                rating=result[2],
            )

        return result

    async def update_one(
        self,
        arg: UpdateOneParams,
    ) -> Author | None:
        query = """
            UPDATE authors SET name = %(name)s, rating = %(rating)s
            WHERE
              id = %(id)s
            RETURNING authors.id AS id, authors.name AS name, authors.rating AS rating
        """

        params = {
            "name": arg.name,
            "rating": arg.rating,
            "id": arg.id,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = Author(
                id=result[0],
                name=result[1],
                rating=result[2],
            )

        return result

    async def update_many(
        self,
        arg: UpdateManyParams,
    ) -> int:
        query = """
            UPDATE authors SET name = %(name)s, rating = %(rating)s
            RETURNING authors.id AS id, authors.name AS name, authors.rating AS rating
        """

        params = {
            "name": arg.name,
            "rating": arg.rating,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = cur.rowcount

        return result

    async def delete(
        self,
        id: int,
    ) -> None:
        query = """
            DELETE FROM authors
            WHERE
              id = %(id)s
        """

        params = {
            "id": id,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def count(
        self,
        id: int,
    ) -> int | None:
        query = """
            SELECT
              COUNT(*) AS count
            FROM authors
            WHERE
              authors.id = %(id)s
        """

        params = {
            "id": id,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result

    async def one_column(
        self,
        id: int,
    ) -> str | None:
        query = """
            SELECT
              authors.name AS name
            FROM authors
            WHERE
              authors.id = %(id)s
        """

        params = {
            "id": id,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result

    async def partial(
        self,
    ) -> list[PartialRow]:
        query = """
            SELECT
              authors.name AS name,
              authors.rating AS rating
            FROM authors
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                PartialRow(
                    name=item[0],
                    rating=item[1],
                )
                async for item in cur
            ]

        return result
