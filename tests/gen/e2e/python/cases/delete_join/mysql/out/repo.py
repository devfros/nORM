"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from asyncmy import Connection


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def delete_join(
        self,
        id: int,
        user_id: int,
    ) -> None:
        query = """
            DELETE   jt,
              pt FROM join_table AS jt
              JOIN primary_table AS pt
                ON jt.primary_table_id = pt.id
            WHERE
              jt.id = %s AND pt.user_id = %s
        """

        params = [
            id,
            user_id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def delete_left_join(
        self,
        id: int,
        user_id: int,
    ) -> None:
        query = """
            DELETE   jt,
              pt FROM join_table AS jt
              LEFT JOIN primary_table AS pt
                ON jt.primary_table_id = pt.id
            WHERE
              jt.id = %s AND pt.user_id = %s
        """

        params = [
            id,
            user_id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def delete_right_join(
        self,
        id: int,
        user_id: int,
    ) -> None:
        query = """
            DELETE   jt,
              pt FROM join_table AS jt
              RIGHT JOIN primary_table AS pt
                ON jt.primary_table_id = pt.id
            WHERE
              jt.id = %s AND pt.user_id = %s
        """

        params = [
            id,
            user_id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def delete_join_with_subquery(
        self,
    ) -> None:
        query = """
            DELETE   pt FROM primary_table AS pt
              JOIN (
                SELECT
                  1 AS id
              ) AS jt
                ON pt.id = jt.id
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)

            result = None

        return result
