"""
Generated by nORM. READ ONLY.
https://github.com/devfros/norm
"""

from dataclasses import dataclass
from psycopg import AsyncConnection as Connection


# Query row schemas
@dataclass
class JoinBarRow:
    id: int
    info: str


@dataclass
class JoinBarAliasRow:
    id: int
    info: str


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def join_bar(
        self,
    ) -> JoinBarRow | None:
        query = """
            SELECT
              f.id AS id,
              b.info AS info
            FROM foo AS f
            LEFT JOIN bar AS b
              ON b.foo_id = f.id
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = await cur.fetchone()
            if result is None:
                return None

            result = JoinBarRow(
                id=result[0],
                info=result[1],
            )

        return result

    async def join_bar_alias(
        self,
    ) -> JoinBarAliasRow | None:
        query = """
            SELECT
              f.id AS id,
              b.info AS info
            FROM foo AS f
            LEFT JOIN bar AS b
              ON b.foo_id = f.id
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = await cur.fetchone()
            if result is None:
                return None

            result = JoinBarAliasRow(
                id=result[0],
                info=result[1],
            )

        return result
