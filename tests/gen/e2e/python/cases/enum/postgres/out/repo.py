"""
Generated by nORM. READ ONLY.
https://github.com/devfros/norm
"""

from psycopg import AsyncConnection as Connection
from .models import Size, User


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def get_all(
        self,
    ) -> list[User]:
        query = """
            SELECT
              users.id AS id,
              users.first_name AS first_name,
              users.last_name AS last_name,
              users.age AS age,
              users.shoe_size AS shoe_size,
              users.shirt_size AS shirt_size
            FROM users
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                User(
                    id=item[0],
                    first_name=item[1],
                    last_name=item[2],
                    age=item[3],
                    shoe_size=item[4],
                    shirt_size=item[5],
                )
                async for item in cur
            ]

        return result

    async def new_user(
        self,
        first_name: str,
        age: int,
        shoe_size: Size,
        last_name: str | None,
        shirt_size: Size | None,
    ) -> None:
        query = """
            INSERT INTO users (
              first_name,
              last_name,
              age,
              shoe_size,
              shirt_size
            )
            VALUES
              (
                %(first_name)s,
                %(last_name)s,
                %(age)s,
                %(shoe_size)s,
                %(shirt_size)s
              )
        """

        params = {
            "first_name": first_name,
            "last_name": last_name,
            "age": age,
            "shoe_size": shoe_size,
            "shirt_size": shirt_size,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def update_sizes(
        self,
        shoe_size: Size,
        id: int,
        shirt_size: Size | None,
    ) -> None:
        query = """
            UPDATE users SET shoe_size = %(shoe_size)s, shirt_size = %(shirt_size)s
            WHERE
              id = %(id)s
        """

        params = {
            "shoe_size": shoe_size,
            "shirt_size": shirt_size,
            "id": id,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def delete_by_size(
        self,
        shoe_size: Size,
        shirt_size: Size,
    ) -> None:
        query = """
            DELETE FROM users
            WHERE
              shoe_size = %(shoe_size)s
              AND shirt_size = %(shirt_size)s
        """

        params = {
            "shoe_size": shoe_size,
            "shirt_size": shirt_size,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result
