"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from psycopg import AsyncConnection as Connection


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def non_null_column(
        self,
        ids: list[int],
    ) -> list[str]:
        query = """
            SELECT
              authors.name AS name
            FROM authors
            WHERE
              authors.id IN (/*LIST1*/)
        """

        params = {}

        # LISTS
        list_params = {
            "LIST1_{}".format(i): item for i, item in enumerate(ids)
        }
        query = query.replace(
            "/*LIST1*/", ", ".join("%({})s".format(k) for k in list_params.keys())
        )
        params.update(list_params)

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                item[0]
                async for item in cur
            ]

        return result

    async def null_column(
        self,
        ratings: list[int],
    ) -> list[str]:
        query = """
            SELECT
              authors.name AS name
            FROM authors
            WHERE
              authors.rating IN (/*LIST1*/)
        """

        params = {}

        # LISTS
        list_params = {
            "LIST1_{}".format(i): item for i, item in enumerate(ratings)
        }
        query = query.replace(
            "/*LIST1*/", ", ".join("%({})s".format(k) for k in list_params.keys())
        )
        params.update(list_params)

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                item[0]
                async for item in cur
            ]

        return result

    async def not_in(
        self,
        ratings: list[int],
    ) -> list[str]:
        query = """
            SELECT
              authors.name AS name
            FROM authors
            WHERE
              NOT authors.id IN (/*LIST1*/)
        """

        params = {}

        # LISTS
        list_params = {
            "LIST1_{}".format(i): item for i, item in enumerate(ratings)
        }
        query = query.replace(
            "/*LIST1*/", ", ".join("%({})s".format(k) for k in list_params.keys())
        )
        params.update(list_params)

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                item[0]
                async for item in cur
            ]

        return result
