"""
Generated by nORM. READ ONLY.
https://github.com/devfros/norm
"""

from psycopg import AsyncConnection as Connection


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def join_where_clause(
        self,
        owner: str,
    ) -> list[int]:
        query = """
            SELECT
              foo.barid AS barid
            FROM foo
            JOIN bar
              ON bar.id = foo.barid
            WHERE
              bar.owner = %(owner)s
        """

        params = {
            "owner": owner,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                item[0]
                async for item in cur
            ]

        return result

    async def join_param_where_clause(
        self,
        bar_id: int,
        owner: str,
    ) -> list[int]:
        query = """
            SELECT
              foo.barid AS barid
            FROM foo
            JOIN bar
              ON bar.id = %(bar_id)s
            WHERE
              bar.owner = %(owner)s
        """

        params = {
            "bar_id": bar_id,
            "owner": owner,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                item[0]
                async for item in cur
            ]

        return result

    async def join_no_constraints(
        self,
        bar_id: int,
        owner: str,
    ) -> list[int]:
        query = """
            SELECT
              foo.barid AS barid
            FROM foo
            CROSS JOIN bar
            WHERE
              bar.id = %(bar_id)s AND bar.owner = %(owner)s
        """

        params = {
            "bar_id": bar_id,
            "owner": owner,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                item[0]
                async for item in cur
            ]

        return result
