"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from contextlib import closing
from datetime import datetime
from duckdb import DuckDBPyConnection as Connection
from pydantic import BaseModel
from .models import Author


# Query row schemas
class GetWithBooksRow(BaseModel):
    id: int
    name: str
    rating: int | None
    id_2: int
    author_id: int
    isbn: str
    book_type: str
    title: str
    year: int
    available: datetime
    tags: list[str]


class PartialRow(BaseModel):
    name: str
    rating: int | None


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    def get_one(
        self,
        id: int,
    ) -> Author | None:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors
            WHERE
              authors.id = ?
        """

        params = [
            id,
        ]

        with closing(self.db.cursor()) as cur:
            cur.execute(query, params)
            result = cur.fetchone()
            if result is None:
                return None

            result = Author(
                id=result[0],
                name=result[1],
                rating=result[2],
            )

        return result

    def get_many(
        self,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors
        """

        with closing(self.db.cursor()) as cur:
            cur.execute(query)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                for item in cur.fetchall()
            ]

        return result

    def get_with_books(
        self,
    ) -> list[GetWithBooksRow]:
        query = """
            SELECT
              a.id AS id,
              a.name AS name,
              a.rating AS rating,
              b.id AS id,
              b.author_id AS author_id,
              b.isbn AS isbn,
              b.book_type AS book_type,
              b.title AS title,
              b.year AS year,
              b.available AS available,
              b.tags AS tags
            FROM authors AS a
            JOIN books AS b
              ON b.author_id = a.id
        """

        with closing(self.db.cursor()) as cur:
            cur.execute(query)
            result = [
                GetWithBooksRow(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                    id_2=item[3],
                    author_id=item[4],
                    isbn=item[5],
                    book_type=item[6],
                    title=item[7],
                    year=item[8],
                    available=item[9],
                    tags=item[10],
                )
                for item in cur.fetchall()
            ]

        return result

    def create(
        self,
        name: str,
        rating: int | None,
    ) -> Author | None:
        query = """
            INSERT INTO authors (
              name,
              rating
            )
            VALUES
              (?, ?)
            RETURNING authors.id AS id, authors.name AS name, authors.rating AS rating
        """

        params = [
            name,
            rating,
        ]

        with closing(self.db.cursor()) as cur:
            cur.execute(query, params)
            result = cur.fetchone()
            if result is None:
                return None

            result = Author(
                id=result[0],
                name=result[1],
                rating=result[2],
            )

        return result

    def update_one(
        self,
        name: str,
        id: int,
        rating: int | None,
    ) -> Author | None:
        query = """
            UPDATE authors SET name = ?, rating = ?
            WHERE
              id = ?
            RETURNING authors.id AS id, authors.name AS name, authors.rating AS rating
        """

        params = [
            name,
            rating,
            id,
        ]

        with closing(self.db.cursor()) as cur:
            cur.execute(query, params)
            result = cur.fetchone()
            if result is None:
                return None

            result = Author(
                id=result[0],
                name=result[1],
                rating=result[2],
            )

        return result

    def update_many(
        self,
        name: str,
        rating: int | None,
    ) -> None:
        query = """
            UPDATE authors SET name = ?, rating = ?
            RETURNING authors.id AS id, authors.name AS name, authors.rating AS rating
        """

        params = [
            name,
            rating,
        ]

        with closing(self.db.cursor()) as cur:
            cur.execute(query, params)

            result = None

        return result

    def delete(
        self,
        id: int,
    ) -> None:
        query = """
            DELETE FROM authors
            WHERE
              id = ?
        """

        params = [
            id,
        ]

        with closing(self.db.cursor()) as cur:
            cur.execute(query, params)

            result = None

        return result

    def count(
        self,
        id: int,
    ) -> int | None:
        query = """
            SELECT
              COUNT(*) AS count
            FROM authors
            WHERE
              authors.id = ?
        """

        params = [
            id,
        ]

        with closing(self.db.cursor()) as cur:
            cur.execute(query, params)
            result = cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result

    def one_column(
        self,
        id: int,
    ) -> str | None:
        query = """
            SELECT
              authors.name AS name
            FROM authors
            WHERE
              authors.id = ?
        """

        params = [
            id,
        ]

        with closing(self.db.cursor()) as cur:
            cur.execute(query, params)
            result = cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result

    def partial(
        self,
    ) -> list[PartialRow]:
        query = """
            SELECT
              authors.name AS name,
              authors.rating AS rating
            FROM authors
        """

        with closing(self.db.cursor()) as cur:
            cur.execute(query)
            result = [
                PartialRow(
                    name=item[0],
                    rating=item[1],
                )
                for item in cur.fetchall()
            ]

        return result
