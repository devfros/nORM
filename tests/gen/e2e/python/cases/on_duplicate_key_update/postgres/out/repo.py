"""
Generated by nORM. READ ONLY.
https://github.com/devfros/norm
"""

from psycopg import AsyncConnection as Connection


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def upsert_author(
        self,
        name: str,
        bio: str | None,
    ) -> None:
        query = """
            INSERT INTO authors (
              name,
              bio
            )
            VALUES
              (%(name)s, %(bio)s) ON CONFLICT(name) DO UPDATE SET bio = %(bio)s
        """

        params = {
            "name": name,
            "bio": bio,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result
