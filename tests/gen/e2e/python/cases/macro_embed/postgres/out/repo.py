"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from psycopg import AsyncConnection as Connection
from pydantic import BaseModel
from .models import Author, Book


# Query row schemas
class NonNullRow(BaseModel):
    id: int
    name: str
    rating: int | None
    book: Book


class NullableRow(BaseModel):
    id: int
    name: str
    rating: int | None
    book: Book | None = None


class EmbedAndNembedRow(BaseModel):
    author: Author
    book: Book | None = None


class AliasedRow(BaseModel):
    foo: Author
    bar: Book | None = None


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def non_null(
        self,
    ) -> list[NonNullRow]:
        query = """
            SELECT
              a.id AS id,
              a.name AS name,
              a.rating AS rating,
              b.id AS id,
              b.author_id AS author_id,
              b.isbn AS isbn,
              b.book_type AS book_type,
              b.title AS title,
              b.year AS year,
              b.available AS available,
              b.tags AS tags
            FROM authors AS a
            JOIN books AS b
              ON b.author_id = a.id
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                NonNullRow(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                    book=Book(
                        id=item[3],
                        author_id=item[4],
                        isbn=item[5],
                        book_type=item[6],
                        title=item[7],
                        year=item[8],
                        available=item[9],
                        tags=item[10],
                    ),
                )
                async for item in cur
            ]

        return result

    async def nullable(
        self,
    ) -> list[NullableRow]:
        query = """
            SELECT
              a.id AS id,
              a.name AS name,
              a.rating AS rating,
              b.id AS id,
              b.author_id AS author_id,
              b.isbn AS isbn,
              b.book_type AS book_type,
              b.title AS title,
              b.year AS year,
              b.available AS available,
              b.tags AS tags
            FROM authors AS a
            LEFT JOIN books AS b
              ON b.author_id = a.id
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                NullableRow(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                    book=Book(
                        id=item[3],
                        author_id=item[4],
                        isbn=item[5],
                        book_type=item[6],
                        title=item[7],
                        year=item[8],
                        available=item[9],
                        tags=item[10],
                    ) if not all(v is None for v in item[3:11]) else None,
                )
                async for item in cur
            ]

        return result

    async def embed_and_nembed(
        self,
    ) -> list[EmbedAndNembedRow]:
        query = """
            SELECT
              a.id AS id,
              a.name AS name,
              a.rating AS rating,
              b.id AS id,
              b.author_id AS author_id,
              b.isbn AS isbn,
              b.book_type AS book_type,
              b.title AS title,
              b.year AS year,
              b.available AS available,
              b.tags AS tags
            FROM authors AS a
            LEFT JOIN books AS b
              ON b.author_id = a.id
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                EmbedAndNembedRow(
                    author=Author(
                        id=item[0],
                        name=item[1],
                        rating=item[2],
                    ),
                    book=Book(
                        id=item[3],
                        author_id=item[4],
                        isbn=item[5],
                        book_type=item[6],
                        title=item[7],
                        year=item[8],
                        available=item[9],
                        tags=item[10],
                    ) if not all(v is None for v in item[3:11]) else None,
                )
                async for item in cur
            ]

        return result

    async def aliased(
        self,
    ) -> list[AliasedRow]:
        query = """
            SELECT
              a.id AS id,
              a.name AS name,
              a.rating AS rating,
              b.id AS id,
              b.author_id AS author_id,
              b.isbn AS isbn,
              b.book_type AS book_type,
              b.title AS title,
              b.year AS year,
              b.available AS available,
              b.tags AS tags
            FROM authors AS a
            LEFT JOIN books AS b
              ON b.author_id = a.id
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                AliasedRow(
                    foo=Author(
                        id=item[0],
                        name=item[1],
                        rating=item[2],
                    ),
                    bar=Book(
                        id=item[3],
                        author_id=item[4],
                        isbn=item[5],
                        book_type=item[6],
                        title=item[7],
                        year=item[8],
                        available=item[9],
                        tags=item[10],
                    ) if not all(v is None for v in item[3:11]) else None,
                )
                async for item in cur
            ]

        return result
