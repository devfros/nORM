"""
Generated by nORM. READ ONLY.
https://github.com/devfros/norm
"""

from psycopg import AsyncConnection as Connection


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def schema_scoped_list(
        self,
    ) -> list[int]:
        query = """
            SELECT
              bar.id AS id
            FROM foo.bar
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                item[0]
                async for item in cur
            ]

        return result

    async def schema_scoped_col_list(
        self,
    ) -> list[int]:
        query = """
            SELECT
              foo.bar.id AS id
            FROM foo.bar
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                item[0]
                async for item in cur
            ]

        return result
