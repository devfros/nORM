"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from psycopg import AsyncConnection as Connection
from .models import User


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def list_explicit_cols(
        self,
    ) -> list[int]:
        query = """
            SELECT
              explicit_cols.x AS x
            FROM app.explicit_cols
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                item[0]
                async for item in cur
            ]

        return result

    async def list_inferred_cols(
        self,
    ) -> list[User]:
        query = """
            SELECT
              inferred_cols.id AS id,
              inferred_cols.name AS name
            FROM app.inferred_cols
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                User(
                    id=item[0],
                    name=item[1],
                )
                async for item in cur
            ]

        return result

    async def list_materialized(
        self,
    ) -> list[int]:
        query = """
            SELECT
              mv.id AS id
            FROM app.mv
        """

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                item[0]
                async for item in cur
            ]

        return result
