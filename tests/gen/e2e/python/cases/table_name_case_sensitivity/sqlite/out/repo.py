"""
Generated by nORM. READ ONLY.
https://github.com/devfros/norm
"""

from aiosqlite import Connection
from .models import Author, Book, User


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def insert_user(
        self,
        name: str | None,
    ) -> None:
        query = """
            INSERT INTO users (
              name
            )
            VALUES
              (?)
        """

        params = [
            name,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def insert_user_mixed_case(
        self,
        name: str | None,
    ) -> None:
        query = """
            INSERT INTO users (
              name
            )
            VALUES
              (?)
        """

        params = [
            name,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def insert_author(
        self,
        name: str | None,
    ) -> None:
        query = """
            INSERT INTO "Authors" (
              name
            )
            VALUES
              (?)
        """

        params = [
            name,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def insert_book(
        self,
        title: str | None,
    ) -> None:
        query = """
            INSERT INTO Books (
              title
            )
            VALUES
              (?)
        """

        params = [
            title,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def update_user(
        self,
        id: int,
        name: str | None,
    ) -> None:
        query = """
            UPDATE users SET name = ?
            WHERE
              id = ?
        """

        params = [
            name,
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def update_user_mixed_case(
        self,
        id: int,
        name: str | None,
    ) -> None:
        query = """
            UPDATE users SET name = ?
            WHERE
              id = ?
        """

        params = [
            name,
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def update_author(
        self,
        id: int,
        name: str | None,
    ) -> None:
        query = """
            UPDATE "Authors" SET name = ?
            WHERE
              id = ?
        """

        params = [
            name,
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def update_book(
        self,
        id: int,
        title: str | None,
    ) -> None:
        query = """
            UPDATE Books SET title = ?
            WHERE
              id = ?
        """

        params = [
            title,
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def delete_user(
        self,
        id: int,
    ) -> None:
        query = """
            DELETE FROM users
            WHERE
              id = ?
        """

        params = [
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def delete_user_mixed_case(
        self,
        id: int,
    ) -> None:
        query = """
            DELETE FROM users
            WHERE
              id = ?
        """

        params = [
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def delete_author(
        self,
        id: int,
    ) -> None:
        query = """
            DELETE FROM "Authors"
            WHERE
              id = ?
        """

        params = [
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def delete_book(
        self,
        id: int,
    ) -> None:
        query = """
            DELETE FROM Books
            WHERE
              id = ?
        """

        params = [
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result

    async def get_user(
        self,
        id: int,
    ) -> User | None:
        query = """
            SELECT
              users.id AS id,
              users.name AS name
            FROM users
            WHERE
              users.id = ?
        """

        params = [
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = User(
                id=result[0],
                name=result[1],
            )

        return result

    async def get_user_mixed_case(
        self,
        id: int,
    ) -> User | None:
        query = """
            SELECT
              users.id AS id,
              users.name AS name
            FROM users
            WHERE
              users.id = ?
        """

        params = [
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = User(
                id=result[0],
                name=result[1],
            )

        return result

    async def get_author(
        self,
        id: int,
    ) -> Author | None:
        query = """
            SELECT
              Authors.id AS id,
              Authors.name AS name
            FROM "Authors"
            WHERE
              Authors.id = ?
        """

        params = [
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = Author(
                id=result[0],
                name=result[1],
            )

        return result

    async def get_book(
        self,
        id: int,
    ) -> Book | None:
        query = """
            SELECT
              Books.id AS id,
              Books.title AS title
            FROM Books
            WHERE
              Books.id = ?
        """

        params = [
            id,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = Book(
                id=result[0],
                title=result[1],
            )

        return result
