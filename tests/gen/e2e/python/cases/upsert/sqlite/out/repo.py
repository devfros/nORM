"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from aiosqlite import Connection


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def upsert_location(
        self,
        name: str,
        address: str,
        zip_code: int,
        latitude: float,
        longitude: float,
    ) -> None:
        query = """
            INSERT INTO locations (
              name,
              address,
              zip_code,
              latitude,
              longitude
            )
            VALUES
              (?, ?, ?, ?, ?) ON CONFLICT(name) DO UPDATE SET name = excluded.name, address = excluded.address, zip_code = excluded.zip_code, latitude = excluded.latitude, longitude = excluded.longitude
        """

        params = [
            name,
            address,
            zip_code,
            latitude,
            longitude,
        ]

        async with self.db.cursor() as cur:
            await cur.execute(query, params)

            result = None

        return result
