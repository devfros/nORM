"""
Generated by nORM. READ ONLY.
https://github.com/devfros/norm
"""

from datetime import datetime
from psycopg import AsyncConnection as Connection
from pydantic import BaseModel
from typing import Literal
from .models import Author


# Query row schemas
class MultipleTablesRow(BaseModel):
    id: int
    name: str
    rating: int | None
    id_2: int
    author_id: int
    isbn: str
    book_type: str
    title: str
    year: int
    available: datetime
    tags: list[str]


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def basic(
        self,
        order_by: Literal['id', 'name', 'rating'],
        desc: bool = False,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors
            ORDER BY
              /*ORDER1*/ NULLS FIRST
        """

        # ORDER BY
        if order_by not in ['id', 'name', 'rating']:
            raise ValueError("Invalid column name '{}'".format(order_by))

        query = query.replace("/*ORDER1*/", "{} {}".format(order_by, 'DESC' if desc else 'ASC'))

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                async for item in cur
            ]

        return result

    async def named(
        self,
        foo: Literal['id', 'name', 'rating'],
        bar: bool = False,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors
            ORDER BY
              /*ORDER1*/ NULLS FIRST
        """

        # ORDER BY
        if foo not in ['id', 'name', 'rating']:
            raise ValueError("Invalid column name '{}'".format(foo))

        query = query.replace("/*ORDER1*/", "{} {}".format(foo, 'DESC' if bar else 'ASC'))

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                async for item in cur
            ]

        return result

    async def multiple(
        self,
        order_by: Literal['id', 'name', 'rating'],
        order_by_2: Literal['id', 'name', 'rating'],
        desc: bool = False,
        desc_2: bool = False,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors
            ORDER BY
              /*ORDER1*/ NULLS FIRST,
              /*ORDER2*/ NULLS FIRST
        """

        # ORDER BY
        if order_by not in ['id', 'name', 'rating']:
            raise ValueError("Invalid column name '{}'".format(order_by))

        query = query.replace("/*ORDER1*/", "{} {}".format(order_by, 'DESC' if desc else 'ASC'))

        if order_by_2 not in ['id', 'name', 'rating']:
            raise ValueError("Invalid column name '{}'".format(order_by_2))

        query = query.replace("/*ORDER2*/", "{} {}".format(order_by_2, 'DESC' if desc_2 else 'ASC'))

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                async for item in cur
            ]

        return result

    async def matching_param(
        self,
        order_by: int,
        order_by_2: Literal['id', 'name', 'rating'],
        desc: bool = False,
    ) -> list[Author]:
        query = """
            SELECT
              authors.id AS id,
              authors.name AS name,
              authors.rating AS rating
            FROM authors
            WHERE
              authors.id = %(order_by)s
            ORDER BY
              /*ORDER1*/ NULLS FIRST
        """

        params = {
            "order_by": order_by,
        }

        # ORDER BY
        if order_by_2 not in ['id', 'name', 'rating']:
            raise ValueError("Invalid column name '{}'".format(order_by_2))

        query = query.replace("/*ORDER1*/", "{} {}".format(order_by_2, 'DESC' if desc else 'ASC'))

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = [
                Author(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                )
                async for item in cur
            ]

        return result

    async def multiple_tables(
        self,
        author_order: Literal['id', 'name', 'rating'],
        book_order: Literal['id', 'author_id', 'isbn', 'book_type', 'title', 'year', 'available', 'tags'],
        author_desc: bool = False,
        book_desc: bool = False,
    ) -> list[MultipleTablesRow]:
        query = """
            SELECT
              a.id AS id,
              a.name AS name,
              a.rating AS rating,
              b.id AS id,
              b.author_id AS author_id,
              b.isbn AS isbn,
              b.book_type AS book_type,
              b.title AS title,
              b.year AS year,
              b.available AS available,
              b.tags AS tags
            FROM authors AS a
            JOIN books AS b
              ON b.author_id = a.id
            ORDER BY
              /*ORDER1*/ NULLS FIRST,
              /*ORDER2*/ NULLS FIRST
        """

        # ORDER BY
        if author_order not in ['id', 'name', 'rating']:
            raise ValueError("Invalid column name '{}'".format(author_order))

        query = query.replace("/*ORDER1*/", "{} {}".format(author_order, 'DESC' if author_desc else 'ASC'))

        if book_order not in ['id', 'author_id', 'isbn', 'book_type', 'title', 'year', 'available', 'tags']:
            raise ValueError("Invalid column name '{}'".format(book_order))

        query = query.replace("/*ORDER2*/", "{} {}".format(book_order, 'DESC' if book_desc else 'ASC'))

        async with self.db.cursor() as cur:
            await cur.execute(query)
            result = [
                MultipleTablesRow(
                    id=item[0],
                    name=item[1],
                    rating=item[2],
                    id_2=item[3],
                    author_id=item[4],
                    isbn=item[5],
                    book_type=item[6],
                    title=item[7],
                    year=item[8],
                    available=item[9],
                    tags=item[10],
                )
                async for item in cur
            ]

        return result
