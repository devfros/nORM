"""
Generated by nORM. READ ONLY.
https://github.com/devfros/nORM
"""

from datetime import date
from psycopg import AsyncConnection as Connection


class Repo:
    def __init__(self, db: Connection) -> None:
        self.db = db

    async def get_total_slack_queries(
        self,
        workspace_id: int,
        start: date,
        end: date,
    ) -> int | None:
        query = """
            SELECT
              COUNT(*) AS count
            FROM astoria.slack_feedback
            WHERE
              astoria.slack_feedback.workspace_id = %(workspace_id)s
              AND slack_feedback.created_at BETWEEN CAST(%(start)s AS DATE) AND CAST(%(end)s AS DATE)
        """

        params = {
            "workspace_id": workspace_id,
            "start": start,
            "end": end,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result

    async def get_total_slack_queries_resolved(
        self,
        workspace_id: int,
        start: date,
        end: date,
    ) -> int | None:
        query = """
            SELECT
              COUNT(*) AS count
            FROM astoria.slack_feedback
            WHERE
              astoria.slack_feedback.workspace_id = %(workspace_id)s
              AND (
                slack_feedback.issue_raised = FALSE
                OR slack_feedback.issue_raised IS NULL
              )
              AND slack_feedback.created_at BETWEEN CAST(%(start)s AS DATE) AND CAST(%(end)s AS DATE)
        """

        params = {
            "workspace_id": workspace_id,
            "start": start,
            "end": end,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result

    async def get_total_slack_queries_requests_created(
        self,
        workspace_id: int,
        start: date,
        end: date,
    ) -> int | None:
        query = """
            SELECT
              COUNT(*) AS count
            FROM astoria.tickets
            WHERE
              astoria.tickets.workspace_id = %(workspace_id)s
              AND tickets.source = 'RAISED_FROM_BOT'
              AND tickets.created_at BETWEEN CAST(%(start)s AS DATE) AND CAST(%(end)s AS DATE)
        """

        params = {
            "workspace_id": workspace_id,
            "start": start,
            "end": end,
        }

        async with self.db.cursor() as cur:
            await cur.execute(query, params)
            result = await cur.fetchone()
            if result is None:
                return None

            result = result[0]

        return result
